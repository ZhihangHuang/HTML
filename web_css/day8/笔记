定位:
    1.可以让元素自由的拜访再网页的任意位置
    2.一边拿用于盒子之间的层叠情况
只加position不加位置 还在原位置

poisition: relative
如果 left right 都有以left为准
    top bottom 都有以top为准
poisition relative
以有定位的父级移动 不论是relative 还是absolute
父类没有移动的话就
